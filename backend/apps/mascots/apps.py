from django.apps import AppConfig


class MascotsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "backend.apps.mascots"
    label = "mascots"
    verbose_name = "Mascots"

